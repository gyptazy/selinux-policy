--- a/Rules.modular
+++ b/Rules.modular
@@ -36,7 +36,7 @@
 #
 # default action: create all module packages
 #
-default: policy
+default: load
 
 all policy: base modules
 
@@ -55,7 +55,7 @@
 # created by semanage
 	@echo "Loading configured modules."
 	@$(INSTALL) -d -m 0755 $(policypath) $(dir $(fcpath))
-	$(verbose) $(SEMODULE) -s $(NAME) -b $(modpkgdir)/$(notdir $(base_pkg)) $(foreach mod,$(mod_pkgs),-i $(modpkgdir)/$(mod))
+	$(verbose) $(SEMODULE) -S /home/vagrant/packaging/selinux-policy/debian/pkg_files/var/lib/selinux/ -s $(NAME) -b $(modpkgdir)/$(notdir $(base_pkg)) $(foreach mod,$(mod_pkgs),-i $(modpkgdir)/$(mod))
 
 ########################################
 #
